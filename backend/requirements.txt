asyncpg==0.30.0

# OAuth
Authlib==1.6.9
azure-ai-documentintelligence==1.0.2
azure-core==1.38.0
azure-functions==1.24.0
azure-identity==1.23.0

# Azure Services
azure-storage-blob==12.28.0b1
bcrypt==4.0.1

# Data processing
beautifulsoup4==4.14.3

#Citation search
biopython==1.87
clean-text==0.7.1
email-validator>=2.0.0
# Core FastAPI dependencies
fastapi>=0.104.1

# Document processing
grobid-client-python==0.1.4
httpx==0.28.1
itsdangerous==2.2.0

# Agent framework dependencies
langchain==1.2.7
langchain-core==1.2.28
langchain-google-genai==4.2.0
openai==2.15.0
pandas==2.2.2
passlib==1.7.4

# Background jobs (Postgres-backed)
# 3.2.0 is no longer published on PyPI (build fails with "No matching distribution")
procrastinate==3.2.2

# Databases
# psycopg2 is used for most blocking DB operations.
psycopg2-binary==2.9.11

# Procrastinate imports psycopg (v3) at import time; install the binary extra so
# the container doesn't need system libpq.
psycopg[binary]>=3.1.0

# psycopg3 is used for async connections (e.g. user DB). The binary extra avoids
# requiring system libpq in slim docker images.
psycopg[binary]==3.3.3
pydantic>=2.7.0
pydantic-settings>=2.3.0
pylint==3.3.8
python-dotenv>=1.0.1

# Security
python-jose[cryptography]>=3.4.0
python-multipart==0.0.26
PyYAML==6.0.2

# Network
requests==2.33.0
rispy==0.10.0
uvicorn>=0.23.2
