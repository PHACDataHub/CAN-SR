# Core FastAPI dependencies
fastapi>=0.104.1
uvicorn>=0.23.2
pydantic>=2.7.0
pydantic-settings>=2.3.0
PyYAML==6.0.2
email-validator>=2.0.0
python-multipart==0.0.22
python-dotenv>=1.0.1
httpx==0.28.1

# Security
python-jose[cryptography]>=3.4.0
passlib==1.7.4
bcrypt==4.0.1

# Azure Services
azure-storage-blob==12.28.0b1
azure-identity==1.23.0
azure-ai-documentintelligence==1.0.2
azure-core==1.38.0
openai==2.15.0

# Databases
# psycopg2 is used for most blocking DB operations.
psycopg2-binary==2.9.11
asyncpg==0.30.0

# Procrastinate imports psycopg (v3) at import time; install the binary extra so
# the container doesn't need system libpq.
psycopg[binary]>=3.1.0

# Background jobs (Postgres-backed)
# 3.2.0 is no longer published on PyPI (build fails with "No matching distribution")
procrastinate==3.2.2

# Data processing
beautifulsoup4==4.14.3
rispy==0.10.0
pylint==3.3.8

# Document processing
grobid-client-python==0.1.4

# Network
requests==2.32.5

# Agent framework dependencies
langchain==1.2.7
langchain-core==1.2.11
langchain-google-genai==4.2.0

# OAuth
Authlib==1.6.7
itsdangerous==2.2.0

#Citation search
biopython==1.86
clean-text==0.7.1
azure-functions==1.24.0
azure-core==1.38.3
