[pytest]
# The repo includes docker volume directories (checked in) that can contain files
# with permissions not readable by the current user (e.g. mongodb diagnostic.data).
# Ignore these paths during test discovery.
norecursedirs =
    backend/volumes
    backend/uploads
    uploads
    planning
